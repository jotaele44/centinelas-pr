{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Moneysweep-PR Intake Record",
  "description": "Financial and political intelligence signals",
  "type": "object",
  "required": ["schema_version", "item_id", "source_url", "title", "labels", "captured_at"],
  "properties": {
    "schema_version": { "type": "string" },
    "item_id": { "type": "string" },
    "source_url": { "type": "string", "format": "uri" },
    "source_name": { "type": "string" },
    "title": { "type": "string" },
    "body_text": { "type": "string" },
    "published_at": { "type": "string", "format": "date-time" },
    "captured_at": { "type": "string", "format": "date-time" },
    "evidence_tier": { "type": "string", "enum": ["T1", "T2", "T3", "T4"] },
    "labels": { "type": "array", "items": { "type": "string" } },
    "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
    "classifier_reasoning": { "type": "string" },
    "routed_to": { "type": "string" },
    "routed_at": { "type": "string", "format": "date-time" },
    "municipalities": { "type": "array", "items": { "type": "string" } },
    "recipients": { "type": "array", "items": { "type": "string" } },
    "agencies": { "type": "array", "items": { "type": "string" } },
    "estimated_value": { "type": ["number", "null"] },
    "signal_stage": { "type": ["string", "null"] },
    "beat": { "type": ["string", "null"] },
    "project_lead": {
      "type": "object",
      "description": "Centinelas discovery envelope. identity_effect is always NONE; MoneySweep must independently bind fiscal records.",
      "required": ["lead_id", "origin_item_id", "identity_effect", "discovery_state"],
      "properties": {
        "lead_id": { "type": "string", "pattern": "^prjlead_[a-f0-9]{32}$" },
        "origin_item_id": { "type": "string" },
        "identity_effect": { "const": "NONE" },
        "discovery_state": { "const": "DETECTED" }
      }
    }
  }
}
