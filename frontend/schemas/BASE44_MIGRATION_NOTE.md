# Base44 Migration Note

The prior Base44 entity schemas were moved to `schemas/entities/` as platform-neutral JSON schema references. The `base44/` runtime directory was removed from the app package.
